{Compute $\ds \frac{d}{dx}\big(\ln (kx)\big) $ two ways: 
\begin{enumerate}
\item		Using the Chain Rule, and 
\item		by first using the logarithm rule $\ln (ab) = \ln a + \ln b$, then taking the derivative.
\end{enumerate}
}
{In both cases the derivative is the same: $1/x$. 
}
