{When evaluating $\iint_R f(x,y)\ dA$ using polar coordinates, $f(x,y)$ is replaced with \underline{\hskip .5in} and $dA$ is replaced with \underline{\hskip.5in}.
}
{$f\big(r\cos\theta,r\sin\theta\big)$, $r\ dr\ d\theta$
}